export default function ShopHome() {
  return (
    <main className="mx-auto max-w-6xl p-6">
      <section className="rounded-theme bg-surface border border-border p-8">
        <h1 className="font-heading text-3xl text-fg">Storefront shell</h1>
        <p className="mt-2 text-muted">Tenant theme and page builder sections render here (Phases 4 and 8).</p>
        <button className="mt-6 rounded-theme bg-primary px-4 py-2 text-primary-fg">Shop now</button>
      </section>
    </main>
  );
}
